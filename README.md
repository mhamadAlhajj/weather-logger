# weather-logger
 A small Python CLI tool that fetches live weather data (OpenWeatherMap), models it with classes, validates and cleans user input with regex, retries failed API calls via decorators, logs results to CSV, and is covered by pytest tests.
